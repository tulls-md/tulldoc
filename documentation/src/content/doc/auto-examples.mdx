---
title: Автопримеры
description: Как tulldoc генерирует примеры из TypeScript-типов пропсов
---

# Автопримеры

Tulldoc автоматически создаёт секцию примеров для каждого пропа компонента,
тип которого - union of literals. Для работы нужен `componentsDir`.

## Как это работает

1. Tulldoc находит исходный файл компонента в `componentsDir`
2. Читает TypeScript-тип первого параметра функции
3. Для каждого пропа с enumerable-типом (`"a" | "b" | "c"`) создаёт секцию
4. Рендерит компонент для каждого значения, передавая `defaultArgs` + текущее значение

## DocAutoExample

Запись в `examples` с полем `prop` настраивает автопример конкретного пропа:

```ts
examples: [
  {
    prop: "variant",
    title: "Варианты",
    description: "Визуальные стили кнопки.",
    view: "grid",
    defaultArgs: { children: "Кнопка" },
  },
];
```

### Поля

| Поле          | Тип              | По умолчанию | Описание                                                 |
| ------------- | ---------------- | ------------ | -------------------------------------------------------- |
| `prop`        | `keyof Props`    | -            | Проп, значения которого перебираются (обязательное)      |
| `title`       | `string`         | имя пропа    | Заголовок секции                                         |
| `description` | `string`         |              | Описание под заголовком (markdown)                       |
| `view`        | `ExampleView`    | `"grid"`     | Раскладка вариантов                                      |
| `defaultArgs` | `Partial<Props>` |              | Переопределяет `defaultArgs` документа для этого примера |
| `disabled`    | `boolean`        | `false`      | `true` - убрать автопример этого пропа                   |

Если задан кастомный `title`, имя пропа не пропадает - оно показывается рядом
с заголовком секции бейджем-ссылкой на строку этого пропа в таблице API.

В `description` поддерживается markdown: `` `код` ``, **жирный**, _курсив_,
[ссылки](/doc/auto-examples) и списки. Упоминание пропа в инлайн-коде
(`` `variant` ``) автоматически становится ссылкой на его строку в таблице API.

## Порядок примеров

Порядок в секции «Примеры» определяется так:

1. Записи из `examples[]` - в том порядке, в котором они написаны
2. Пропсы, не упомянутые в `examples[]`, - в порядке объявления в TypeScript-типе

Если проп упомянут в `examples[]` с `disabled: true`, он полностью пропускается.

## Раскладки ExampleView

Варианты рендерятся на точечном канвасе с отступами - едином фоне всех превью.

### `"grid"` (по умолчанию)

Все варианты в одном превью, в ряд с переносом. Для небольших компонентов.

### `"cell"`

Варианты друг под другом на всю ширину. Для крупных или растягивающихся компонентов.

### `"plain"`

Раскладка как в `"grid"`, но без точечного канваса и отступов. Для примеров,
которым фон мешает (полноширинные и полноэкранные компоненты).

## Поддерживаемые типы пропсов

| Тип                 | Результат                     |
| ------------------- | ----------------------------- |
| `"a" \| "b" \| "c"` | Три варианта: `a`, `b`, `c`   |
| `boolean`           | Два варианта: `true`, `false` |
| `1 \| 2 \| 3`       | Три варианта: `1`, `2`, `3`   |
| `string`            | Ошибка - тип не enumerable    |
| `number`            | Ошибка - тип не enumerable    |
| `ReactNode`         | Ошибка - тип не enumerable    |

Если для пропа нельзя перечислить значения, tulldoc выводит сообщение об ошибке
прямо в секции примера (в блоке `DocNotice`). Этот проп не блокирует остальные.

## Отключение автопримера

Если автопример для конкретного пропа не нужен:

```ts
examples: [
  { prop: "disabled", disabled: true },
  { prop: "loading", disabled: true },
];
```

## Пример полного использования

```ts
export default function (): DocMeta<typeof Button> {
  return {
    component: Button,
    defaultArgs: { children: "Кнопка" },
    examples: [
      {
        prop: "variant",
        title: "Стиль",
        view: "grid",
      },
      {
        prop: "size",
        title: "Размер",
        defaultArgs: { children: "Текст кнопки" }, // переопределяет defaultArgs документа
      },
      { prop: "disabled", disabled: true }, // убираем автопример для disabled
    ],
  };
}
```
