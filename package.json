{
  "name": "tulldoc-monorepo",
  "private": true,
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  },
  "scripts": {
    "tulldoc:docs": "pnpm --filter documentation dev",
    "tulldoc:example": "pnpm --filter example dev",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
